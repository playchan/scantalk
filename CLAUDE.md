# CLAUDE.md

이 문서는 Claude Code가 VS Code 환경에서 이 프로젝트를 작업할 때 반드시 따라야 하는 최상위 지침서다.

## 1. 프로젝트 개요

이 프로젝트는 AI 연애 분석·매칭 MVP 모바일 웹앱이다.

처음부터 완성형 소개팅 앱을 만드는 것이 아니라, 사용자가 모바일 웹에서 연애 성향 테스트와 카톡/썸 상황 분석을 신청하고, 관리자가 AI 분석 초안을 검수한 뒤 사용자에게 리포트를 공개하는 1차 MVP를 만든다.

핵심 흐름은 다음과 같다.

사용자 유입
→ 랜딩페이지
→ 회원가입 / 로그인
→ 연애 성향 테스트
→ 성향 리포트 확인
→ 카톡 분석 신청
→ 이미지 업로드
→ 결제 상태 수동 확인
→ 관리자 AI 초안 생성
→ 관리자 최종 검수
→ 사용자 리포트 확인
→ 매칭 신청

v2(라운드제)부터는 무료 바이럴 검사가 유입 엔진으로 추가된다.

바이럴 검사 유입 (비로그인)
→ "내가 결혼할 확률은?" 12문항
→ 결과 미리보기 → 간편 가입
→ 결과 확인 + 공유 카드
→ 매칭 사전신청

v2 전체 전략과 라운드 계획은 docs/09-viral-ai-matching-plan.md,
라운드 1 상세 명세는 docs/10-v2-mvp-spec.md를 기준으로 한다.

## 2. 작업 환경

이 프로젝트는 VS Code에서 작업한다.

기본 작업 방식은 다음과 같다.

- VS Code에서 프로젝트 폴더를 연다.
- Claude Code는 VS Code 확장 또는 VS Code 통합 터미널에서 실행한다.
- 모든 설계 문서는 docs 폴더에 저장한다.
- 모든 개발 판단은 이 CLAUDE.md를 기준으로 한다.
- Cursor 전용 파일인 .cursor/rules는 사용하지 않는다.

## 3. 기술 스택

이 프로젝트는 다음 기술 스택을 사용한다.

- Next.js App Router
- TypeScript
- Tailwind CSS
- Supabase
- Supabase Auth
- Supabase Storage
- OpenAI API
- Vercel
- GitHub
- VS Code
- Claude Code

다른 프레임워크나 라이브러리를 임의로 추가하지 않는다.

새로운 패키지가 필요할 경우, 먼저 왜 필요한지 설명하고 기존 스택으로 해결 가능한지 검토한다.

## 4. 1차 MVP 필수 기능

1차 MVP에는 아래 기능만 포함한다.

### 사용자 기능

- 랜딩페이지
- 회원가입
- 로그인
- 로그아웃
- 마이페이지
- AI 연애 성향 테스트
- 성향 테스트 결과 저장
- 카톡 분석 신청
- 관계 상황 입력
- 고민 내용 입력
- 카톡 캡처 이미지 업로드
- 개인정보 가림 체크
- 상품 선택
- 분석 신청 완료
- 결제 안내
- 분석 결과 확인
- 매칭 신청
- 바이럴 무료 검사 (비로그인 진행, 라운드 1)
- 검사 결과 공유 카드
- 매칭 사전신청

### 관리자 기능

- 관리자 권한 확인
- 분석 신청 목록 조회
- 분석 신청 상세 조회
- 업로드 이미지 확인
- 결제 상태 변경
- 처리 상태 변경
- AI 분석 초안 생성
- 운영자 수정본 입력
- 최종 리포트 저장
- 리포트 공개 여부 변경
- 매칭 신청자 조회
- 인사이트 대시보드 v1 (유입·전환 KPI, 퍼널, 채널별 — /admin/insights)

## 5. 1차 MVP 제외 기능

아래 기능은 1차 MVP에서 만들지 않는다.

- 앱스토어 출시
- 네이티브 앱 개발
- 실시간 채팅
- 자동 매칭 알고리즘
- 커뮤니티
- 음성 대화
- AI 캐릭터 대화
- 위치 기반 추천
- 연락처 기반 추천
- 카카오톡 OCR 자동 분석
- 푸시 알림
- 토스페이먼츠 자동 결제 API
- 카카오 로그인
- 추천인 시스템
- 포인트 시스템
- 고도화된 관리자 통계 대시보드 (단, 라운드 1의 인사이트 v1 — KPI·퍼널·채널 집계 — 는 포함)
- AI봇 생성·봇 대화·호감도 시스템 (라운드 2~3에서 착수, docs/09 참고)

이 기능들은 유료 결제 30건 이상, 성향 테스트 100건 이상, 매칭 신청자 100명 이상이 쌓인 뒤 검토한다.

## 6. 결제 정책

초기 MVP에서는 자동 결제 API를 붙이지 않는다.

초기 결제 흐름은 다음과 같다.

1. 사용자가 분석 신청을 한다.
2. 신청 완료 화면에서 결제 링크를 안내한다.
3. 사용자가 결제 링크 또는 계좌이체로 결제한다.
4. 사용자가 카카오채널로 결제 정보를 보낸다.
5. 관리자가 결제 여부를 확인한다.
6. 관리자가 payment_status를 paid로 변경한다.
7. 관리자가 AI 분석을 진행한다.

초기 개발에서는 결제 API보다 payment_status 관리 기능을 우선한다.

## 7. 이미지 업로드 정책

카톡 캡처 이미지는 민감한 개인정보를 포함할 수 있다.

따라서 다음 원칙을 따른다.

- Supabase Storage의 private bucket을 사용한다.
- public bucket을 사용하지 않는다.
- 사용자가 업로드 전 개인정보 가림 체크를 해야만 신청 가능하게 한다.
- 안내 문구를 반드시 표시한다.
- 상대방 이름, 얼굴, 전화번호, 카카오톡 ID, 계정 정보는 가리고 업로드하도록 안내한다.
- 관리자가 아닌 사용자는 타인의 이미지에 접근할 수 없어야 한다.

## 8. AI 분석 정책

AI 분석은 상대방의 마음을 확정하는 기능이 아니다.

다음 표현은 금지한다.

- "상대는 당신을 좋아합니다."
- "상대는 관심이 없습니다."
- "이 사람은 반드시 돌아옵니다."
- "이렇게 하면 상대를 조종할 수 있습니다."
- "상대의 심리를 완전히 알 수 있습니다."

다음과 같이 가능성 중심으로 표현한다.

- "호감 가능성이 있는 신호가 일부 보입니다."
- "현재 대화 흐름상 관심이 유지되고 있을 가능성이 있습니다."
- "다만 단정하기는 어렵고, 추가 대화에서 확인이 필요합니다."
- "상대방을 압박하기보다 가볍게 반응을 확인하는 방향이 좋습니다."

## 9. 안전 정책

다음 목적의 요청은 분석을 제한하거나 거부한다.

- 스토킹
- 불법 추적
- 위치 추적
- 개인정보 찾기
- 연락처 알아내기
- 미성년자 관련 부적절한 대화
- 불법 촬영
- 협박
- 집착 강화
- 상대방 조작
- 원치 않는 만남 강요
- 보복성 메시지 작성

AI 분석 결과는 건강한 관계, 존중 있는 대화, 사용자의 감정 안정, 안전한 거리두기를 우선한다.

## 10. 데이터베이스 기본 테이블

초기 MVP 기준 주요 테이블은 다음과 같다.

- users
- profiles
- analysis_requests
- analysis_reports
- payments
- match_applications
- matches

v2 라운드 1에서 추가된 테이블:

- quiz_results (바이럴 검사 결과, 4축 성향 포함)
- preregistrations (매칭 사전신청, 1인 1건)
- events (퍼널 이벤트 — service_role 전용, RLS 정책 없음)

테이블을 추가하기 전에는 기존 테이블로 해결 가능한지 먼저 검토한다.

## 11. 권한 정책

권한 처리는 매우 중요하다.

다음 원칙을 반드시 지킨다.

- 로그인하지 않은 사용자는 dashboard, analysis, matching 페이지에 접근할 수 없다.
- 일반 사용자는 본인의 데이터만 볼 수 있다.
- 일반 사용자는 타인의 analysis_reports에 접근할 수 없다.
- is_published가 false인 리포트는 사용자에게 보이면 안 된다.
- 관리자 페이지는 role이 admin인 사용자만 접근할 수 있다.
- service_role key는 클라이언트 코드에 절대 노출하지 않는다.
- 환경변수는 .env.local 또는 Vercel 환경변수에서만 관리한다.

## 12. 코드 작성 원칙

코드를 작성할 때 다음 원칙을 따른다.

- TypeScript를 사용한다.
- Next.js App Router 구조를 따른다.
- Tailwind CSS로 UI를 작성한다.
- 모바일 화면을 우선한다.
- 컴포넌트는 재사용 가능하게 분리한다.
- API Key는 클라이언트 컴포넌트에 노출하지 않는다.
- Supabase client/server 역할을 분리한다.
- 에러 메시지는 사용자에게 이해하기 쉽게 표시한다.
- 기능 구현 후 빌드 에러 가능성을 점검한다.

## 13. VS Code 작업 원칙

VS Code에서는 다음 방식으로 작업한다.

- 프로젝트 루트에서 VS Code를 연다.
- 통합 터미널을 사용한다.
- npm 명령어는 VS Code 터미널에서 실행한다.
- Claude Code에게 작업을 시킬 때는 먼저 관련 파일을 읽게 한다.
- 대규모 수정 전에는 현재 구조를 먼저 요약하게 한다.
- 기능 구현 후에는 npm run build로 검수한다.
- 변경된 파일 목록을 반드시 설명하게 한다.

## 14. Claude Code 작업 방식

작업을 시작할 때는 먼저 현재 폴더 구조와 문서를 읽는다.

반드시 확인할 문서:

- CLAUDE.md
- docs/01-PRD.md
- docs/02-architecture.md
- docs/03-db-schema.md
- docs/04-ui-flow.md
- docs/05-security-checklist.md
- docs/06-business-model.md
- docs/07-roadmap.md
- docs/08-ai-prompt-guide.md
- docs/09-viral-ai-matching-plan.md (v2 전략·라운드 계획)
- docs/10-v2-mvp-spec.md (라운드 1 상세 명세)

작업 전에는 다음을 먼저 판단한다.

1. 지금 요청이 MVP 범위에 포함되는가?
2. 제외 기능을 만들라는 요청은 아닌가?
3. 개인정보나 보안 위험은 없는가?
4. 기존 파일을 수정해야 하는가, 새 파일을 만들어야 하는가?
5. DB 구조 변경이 필요한가?
6. 관리자 검수 흐름이 유지되는가?

## 15. Claude Code 금지 행동

Claude Code는 다음 행동을 하지 않는다.

- 요청 없이 대규모 리팩토링을 하지 않는다.
- 요청 없이 기술 스택을 바꾸지 않는다.
- 요청 없이 결제 API를 붙이지 않는다.
- 요청 없이 OCR 기능을 붙이지 않는다.
- 요청 없이 자동 매칭을 만들지 않는다.
- 요청 없이 실시간 채팅을 만들지 않는다.
- 요청 없이 DB 테이블을 과도하게 추가하지 않는다.
- 요청 없이 디자인 시스템을 과하게 만들지 않는다.
- .env.local 값을 출력하거나 노출하지 않는다.
- service_role key를 클라이언트 파일에 넣지 않는다.
- 사용자가 타인의 리포트를 볼 수 있는 코드를 만들지 않는다.

## 16. 설계서 작성 원칙

설계서를 작성할 때는 다음 순서로 만든다.

1. 서비스 목적
2. 핵심 사용자
3. MVP 범위
4. 제외 범위
5. 사용자 플로우
6. 관리자 플로우
7. 데이터베이스 구조
8. 페이지 구조
9. API 구조
10. AI 프롬프트 구조
11. 권한 정책
12. 개인정보 보호 정책
13. 개발 순서
14. 테스트 체크리스트
15. 배포 순서
16. 운영 방식

설계서는 비개발자도 이해할 수 있게 작성한다.
개발 용어를 사용할 경우 쉬운 설명을 함께 붙인다.

## 17. 개발 우선순위

개발은 반드시 아래 순서로 진행한다.

1. 프로젝트 초기 세팅
2. 기본 라우팅
3. Supabase 연결
4. 회원가입 / 로그인
5. 마이페이지
6. 랜딩페이지
7. 성향 테스트
8. 성향 분석 API
9. 카톡 분석 신청
10. 이미지 업로드
11. 신청 완료 / 결제 안내
12. 관리자 신청 목록
13. 관리자 신청 상세
14. 결제 상태 변경
15. AI 분석 초안 생성
16. 리포트 저장
17. 리포트 공개
18. 사용자 결과 확인
19. 매칭 신청
20. Vercel 배포
21. 실제 유료 테스트

## 18. 테스트 기준

기능을 만들면 다음을 확인한다.

- npm run dev가 정상 실행되는가
- npm run build가 통과하는가
- 로그인하지 않은 사용자의 접근이 차단되는가
- 일반 사용자가 타인의 리포트를 볼 수 없는가
- 관리자만 admin 페이지에 접근하는가
- 이미지 업로드가 private bucket에 저장되는가
- payment_status 변경이 정상 작동하는가
- is_published가 false인 리포트가 숨겨지는가
- API Key가 노출되지 않는가

## 19. 응답 방식

Claude Code는 작업 결과를 설명할 때 다음 형식을 따른다.

1. 수정한 파일
2. 변경한 이유
3. 주의해야 할 점
4. 다음 작업 추천
5. 사용자가 직접 확인해야 할 테스트

비개발자가 이해할 수 있도록 설명한다.

## 20. 최종 목표

이 프로젝트의 최종 목표는 완벽한 앱 출시가 아니라, 빠르게 MVP를 만들어 실제 고객이 돈을 내는지 검증하는 것이다.

따라서 모든 개발 판단은 다음 질문을 기준으로 한다.

"이 기능이 지금 유료 결제 검증에 직접 도움이 되는가?"

도움이 되지 않으면 1차 MVP에서는 제외한다.
